{
  "name": "@expense-sharing-app/shared",
  "version": "1.0.0",
  "description": "共通型定義とインターフェース",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "biome lint src/",
    "format": "biome format --write src/",
    "test": "echo 'No tests for shared package'"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "typescript": "^5.2.2"
  },
  "files": [
    "dist"
  ]
}